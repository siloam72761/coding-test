# 5-8 DFS 예제

## ✅ 핵심 아이디어
2차원 리스트로 각 노드가 연결된 정보를 표한한다. graph[[]]
방문 처리 처리된 걸 관리하는 변수를 visited로 관리한다.

## 🧠 배운점
visited[v] = True를 재귀 호출 전에 해줘야 루프에 빠지지 않는다.
DFS는 “깊게 들어가고, 막히면 되돌아온다(백트래킹)

## ⏱️ 복잡도
시간 복잡도는 O(V + E) 
V: 정점(노드) 수, E: 간선(연결) 수